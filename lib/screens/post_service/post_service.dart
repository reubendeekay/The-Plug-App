import 'package:flutter/material.dart';

class PostServiceScreen extends StatelessWidget {
  const PostServiceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('To be completed'),
      ),
    );
  }
}
